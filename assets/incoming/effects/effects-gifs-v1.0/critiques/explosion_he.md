Verdict: PASS

- Whole-family generation: the accepted source is the second and final permitted complete-family attempt. The first 4x2 generation was rejected before processing because its alpha topology had no plausible middle vertical gutter. The targeted retry kept the same event and strengthened only transparent separation; no individual frame was generated, edited, patched, trimmed, resized, or independently recentered.
- Viewpoint and sequence: all eight frames read in row-major order as one strict top-down heavy detonation: compact white-hot ignition (01), rapid orange fireball and outward tan dust/debris ring (02–03), fire being swallowed by dense brown-grey dust (04–06), then hanging and thinning dust (07–08). There is no horizon, visible ground plane, text, label, divider, or watermark.
- Heavy-event identity: compared with the accepted fragmentation pilot, the HE family has a broader, denser, dirtier dust footprint, larger debris chunks, substantially more persistent volume, and a slower decay. The wide ground-hugging ring in 02–03 and the churning dust mass in 04–06 distinguish it from a hand grenade.
- Stability and isolation: alpha-above-8 centroids remain close to the 512px cell centre—x `251.83–264.67`, y `242.19–251.65`—without distracting jumps. Every alpha-above-8 bounding box stays within its cell; none is clipped. The accepted source has inferable transparent separation runs at x `355–410`, `742–777`, `1144–1172` and y `468–556`, with no alpha above 8 in those bands. Normalization preserved all `648,078` occupied source pixels as one family.
- PNG and chroma QA: the eight canonical 512x512 RGBA PNGs are byte-for-byte pixel-equivalent to their normalized-source cells. Checkerboard and black composites show clean neutral tan/brown-grey edges without opaque magenta panels or material green, rose, or olive fringe. The public 2048x1024 RGB source sheet has exact `#FF00FF` at all four corners; the internal normalized RGBA sheet remains the processing source of truth.
- Serialized-GIF QA: the actual reopened GIF—not a PNG surrogate—was composited over a checkerboard. Opaque rose-like (`R > G` and `B >= G`) and olive-like (`R == G > B`) counts are both `[0, 0, 0, 0, 0, 0, 0, 0]`. Expected and actual ordered-mask occupancy match exactly at `[16096, 51229, 82942, 73764, 70328, 70592, 68316, 47971]`, so serialization introduces no topology mismatch.
- Timing and scale: the GIF decodes as eight 512x512 frames with `loop=0`, transparency index `0`, and durations `[80, 80, 90, 110, 140, 180, 220, 300]` ms. The fast impact and increasingly long dust holds make the one-shot event feel heavier and slower while retaining a readable forward progression. `world_diameter_px: 96` records the intended three-tile runtime display diameter (3 x 32 world pixels) without resizing the preserved native 512px cells.
- Preview semantics: this is a non-seamless event preview; its repeated last-to-first transition is not an art gate. Only the forward white-hot-core through settling-dust sequence is accepted.
